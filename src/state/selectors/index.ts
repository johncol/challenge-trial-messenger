import { userSelector as user } from './user';
import { friendsSelector as friends } from './friends';
import { messagesSelector as messages } from './messages';
import { rootSelector as root } from './root';

export const Selectors = {
  root,
  user,
  friends,
  messages
};
